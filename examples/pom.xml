<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.github.naofum</groupId>
  <artifactId>thinreports-java-examples</artifactId>
  <version>1.0.1</version>
  <packaging>jar</packaging>

  <name>thinreports-java-examples</name>
  <description>Examples for thinreports-java: Java ports of the thinreports-examples samples (text-block, image-block, event, eudc, multiple-layout, permission, estimate/estimate-ja, list basic/advanced/group-rows) plus barcode / QR / chart image generation fed into image-block elements.</description>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.11.0</junit.version>
  </properties>

  <dependencies>
    <!-- The library under demonstration. Install it first: mvn -f ../pom.xml install -->
    <dependency>
      <groupId>com.github.naofum</groupId>
      <artifactId>thinreports-java</artifactId>
      <version>1.0.1</version>
    </dependency>

    <!-- Barcode / QR image generation -->
    <dependency>
      <groupId>com.singingbush</groupId>
      <artifactId>barcode4j</artifactId>
      <version>2.4.0</version>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
      <version>3.5.3</version>
    </dependency>

    <!-- Chart image generation -->
    <dependency>
      <groupId>org.jfree</groupId>
      <artifactId>jfreechart</artifactId>
      <version>1.5.6</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.0</version>
      </plugin>
    </plugins>
  </build>
</project>
